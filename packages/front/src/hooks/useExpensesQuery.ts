// import { fetcher } from '@/client/fetcher'
// import { useQuery, useQueryClient } from 'react-query'

// export const useExpensesQuery = (userId: string) => {
//   const queryClient = useQueryClient()
//   const { data } = useQuery(['expenses'], {
//     queryFn: () => fetcher(`/api/expenses?userId=${userId}&page=`),
//     suspense: true,
//   })
// }
